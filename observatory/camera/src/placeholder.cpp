// Placeholder translation unit; keeps observatory_camera a real STATIC
// library from the start so future .cpp files here are additive, not a
// target-type change.
namespace observatory::camera {}
